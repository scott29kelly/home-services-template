---
phase: 03-lead-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/config/forms.ts
  - src/config/features.ts
  - src/lib/form-handler.ts
  - src/lib/schemas.ts
  - src/components/forms/ContactForm.tsx
  - src/pages/Contact.tsx
  - src/pages/ThankYou.tsx
  - src/App.tsx
autonomous: true
requirements: [LEAD-01, LEAD-02]

must_haves:
  truths:
    - "Contact form submits data to Formspree (or configured backend) and redirects to /thank-you"
    - "All required fields show inline validation errors on blur with accessible announcements"
    - "Honeypot field is present but invisible to real users and screen readers"
    - "Thank-you page shows next steps, business hours, and emergency contact info"
    - "Form loading/error states display correctly during submission"
  artifacts:
    - path: "src/lib/form-handler.ts"
      provides: "Backend-agnostic form submission (Formspree/webhook/none)"
      exports: ["submitForm"]
    - path: "src/lib/schemas.ts"
      provides: "Zod validation schemas for contact and booking forms"
      exports: ["contactSchema", "bookingSchema", "ContactFormData", "BookingFormData"]
    - path: "src/components/forms/ContactForm.tsx"
      provides: "RHF + Zod contact form with inline validation"
      min_lines: 80
    - path: "src/pages/ThankYou.tsx"
      provides: "Post-submission confirmation page with next steps"
      min_lines: 40
    - path: "src/config/forms.ts"
      provides: "Extended form config with submission provider, field definitions, thank-you content"
      contains: "submission"
  key_links:
    - from: "src/components/forms/ContactForm.tsx"
      to: "src/lib/form-handler.ts"
      via: "submitForm() call in onSubmit handler"
      pattern: "submitForm"
    - from: "src/components/forms/ContactForm.tsx"
      to: "src/lib/schemas.ts"
      via: "zodResolver(contactSchema) in useForm"
      pattern: "zodResolver.*contactSchema"
    - from: "src/components/forms/ContactForm.tsx"
      to: "/thank-you"
      via: "navigate('/thank-you') on successful submission"
      pattern: "navigate.*thank-you"
    - from: "src/App.tsx"
      to: "src/pages/ThankYou.tsx"
      via: "lazy import + Route element"
      pattern: "ThankYou"
---

<objective>
Build the form submission infrastructure (form-handler, Zod schemas, config extension) and refactor the Contact page to use React Hook Form with Zod validation. Create the Thank You page for post-submission redirect.

Purpose: The contact form is the primary lead conversion mechanism. This plan makes it actually submit data (instead of the current no-op) with proper validation, error handling, and a conversion-tracking-friendly redirect to /thank-you.

Output: Working contact form that validates fields inline, submits to Formspree/webhook, and redirects to a useful Thank You page.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lead-generation/03-RESEARCH.md
@src/config/forms.ts
@src/config/features.ts
@src/config/index.ts
@src/pages/Contact.tsx
@src/App.tsx
@src/components/layout/Layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create form infrastructure</name>
  <files>
    package.json
    src/config/forms.ts
    src/config/features.ts
    src/lib/form-handler.ts
    src/lib/schemas.ts
  </files>
  <action>
    **1. Install react-hook-form and @hookform/resolvers:**
    ```bash
    npm install react-hook-form @hookform/resolvers
    ```
    Note: zod is already installed (v4.3.6).

    **2. Extend `src/config/forms.ts`** with submission config and structured field definitions.
    Add a `submission` block with `provider` ('formspree' | 'webhook' | 'netlify' | 'none'), `formspreeId` (empty string placeholder), and `webhookUrl` (empty string placeholder). Restructure the `contact` section to include `fields` with `firstName`, `lastName`, `email`, `phone`, `address`, `service`, `referral`, `message` -- each with `label`, `placeholder`, and `required` boolean. Add `submittingText: 'Sending...'`. Add a `booking` section with `heading`, `subheading`, `submitText`, `submittingText`, `timeRanges` array (['Morning (8am-12pm)', 'Afternoon (12pm-4pm)', 'Evening (4pm-7pm)']), `blockedDays` ([0] for Sunday), and `maxDaysOut` (60). Add a `thankYou` section with `heading`, `subheading`, and `nextSteps` array. Keep backward compatibility with the existing `contact.heading`, `contact.subheading`, `contact.submitText`, `contact.successMessage` fields -- the existing code may reference them until Contact.tsx is refactored in Task 2.

    **3. Update `src/config/features.ts`** -- change feature flag comments to match actual phase:
    - `financingCalculator` comment should say "Phase 3" (not Phase 4)
    - `onlineBooking` comment should say "Phase 3" (not Phase 4)
    - Leave both as `false` for now (Plan 02 and Plan 03 will enable them)

    **4. Create `src/lib/form-handler.ts`** -- backend-agnostic submission function:
    ```typescript
    import { forms } from '../config/forms'
    type SubmissionResult = { ok: true } | { ok: false; error: string }
    export async function submitForm(data: Record<string, unknown>): Promise<SubmissionResult>
    ```
    Implementation must:
    - Check honeypot field (`_gotcha`) -- if filled, silently return `{ ok: true }` (it's a bot)
    - Remove `_gotcha` from the payload before sending
    - Support providers: `formspree` (POST to `https://formspree.io/f/{formspreeId}` with JSON + Accept header), `webhook` (POST to `webhookUrl` with JSON), `netlify` (POST to form action), `none` (console.log + return ok)
    - Wrap in try/catch, return `{ ok: false, error: 'Something went wrong. Please try again or call us directly.' }` on failure
    - When `formspreeId` is empty and provider is 'formspree', fall back to 'none' behavior (console.log)

    **5. Create `src/lib/schemas.ts`** -- Zod validation schemas:
    - `contactSchema`: firstName (min 1), lastName (min 1), email (email format), phone (regex for US format: `/^\(?[0-9]{3}\)?[-.\s]?[0-9]{3}[-.\s]?[0-9]{4}$/`), address (optional string), service (min 1 "Please select a service"), referral (optional string), message (optional string), _gotcha (optional string for honeypot)
    - `bookingSchema`: firstName (min 1), lastName (min 1), email (email), phone (US regex), address (optional), preferredDate (string min 1 "Please select a date"), preferredTime (string min 1 "Please select a time"), service (min 1), notes (optional), _gotcha (optional)
    - Export inferred types: `ContactFormData`, `BookingFormData`
    - Use clear, user-friendly error messages: "First name is required", "Please enter a valid email address", "Please enter a valid phone number (e.g., 555-123-4567)"
  </action>
  <verify>
    - `npm ls react-hook-form` shows installed
    - `npm ls @hookform/resolvers` shows installed
    - `npx tsc --noEmit` passes (no type errors in new files)
    - `src/lib/form-handler.ts` exports `submitForm`
    - `src/lib/schemas.ts` exports `contactSchema`, `bookingSchema`, `ContactFormData`, `BookingFormData`
  </verify>
  <done>
    react-hook-form and @hookform/resolvers installed. forms.ts extended with submission config and structured fields. form-handler.ts provides backend-agnostic submitForm(). schemas.ts provides Zod schemas for both contact and booking forms. features.ts comments corrected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor Contact page with RHF + create Thank You page</name>
  <files>
    src/components/forms/ContactForm.tsx
    src/pages/Contact.tsx
    src/pages/ThankYou.tsx
    src/App.tsx
  </files>
  <action>
    **1. Create `src/components/forms/ContactForm.tsx`** -- extracted form component using React Hook Form:
    - Import `useForm` from 'react-hook-form', `zodResolver` from '@hookform/resolvers/zod', `contactSchema` + `ContactFormData` from '../../lib/schemas', `submitForm` from '../../lib/form-handler', `useNavigate` from 'react-router-dom'
    - Configure useForm with `resolver: zodResolver(contactSchema)` and `mode: 'onTouched'` (validate on first blur, then on change)
    - If zodResolver type errors occur with Zod v4, try explicit type annotation: `useForm<ContactFormData>({ resolver: zodResolver(contactSchema) })`. If that still fails, use `standardSchemaResolver` from `@hookform/resolvers/standard-schema` as fallback per research.
    - Service options: import from services config (`services.map(s => s.navLabel)`) plus 'Free Roof Inspection', 'Insurance Claim Help', 'Other'
    - Referral options: hardcode same list as current Contact.tsx
    - Each field uses `{...register('fieldName')}` (uncontrolled -- do NOT use controlled inputs with RHF)
    - Each field has: `aria-invalid={!!errors.fieldName}`, `aria-describedby={errors.fieldName ? 'fieldName-error' : undefined}`
    - Error messages rendered with `role="alert"` and `id="fieldName-error"`, styled `text-red-600 text-sm mt-1`
    - Honeypot field: `<input {...register('_gotcha')} tabIndex={-1} autoComplete="off" style={{ position: 'absolute', left: '-9999px', opacity: 0, height: 0, overflow: 'hidden' }} aria-hidden="true" />`
    - Submit button shows `isSubmitting` state: disabled + "Sending..." text
    - On submit: call `submitForm(data)`. If `result.ok`, call `navigate('/thank-you')`. If not ok, set a local error state and show error message above submit button with retry option.
    - Match the existing Contact.tsx styling exactly: same Tailwind classes for inputs (bg-surface, border-border, rounded-xl, focus:ring-brand-blue/30), same gradient orange submit button, same grid layout (2-col for name fields, 2-col for email/phone, full-width address, 2-col for service/referral, full-width message)

    **2. Refactor `src/pages/Contact.tsx`:**
    - Remove the inline form and `useState(submitted)` + `handleSubmit` logic
    - Remove the hardcoded `serviceOptions` and `referralOptions` arrays (moved to ContactForm.tsx)
    - Import and render `<ContactForm />` in place of the old form
    - Keep: Hero section, contact info sidebar, FAQ section -- unchanged
    - Keep: useScrollReveal for the animation wrapper
    - Leave the two-mode UI (message form + booking tab) preparation: for now render ContactForm as the single mode. Plan 02 will add the booking tab. Structure the JSX so a tab UI can be added around the form area without rewriting the page.
    - Move FAQ data to a separate const or keep inline (Claude's discretion on cleanest approach)

    **3. Create `src/pages/ThankYou.tsx`:**
    - Render Hero with compact mode, appropriate headline ("Thank You!" / "Request Received")
    - Main content section with: success icon (CheckCircle from lucide-react), heading and subheading from forms.thankYou config, numbered next-steps list from config, business hours, phone number (click-to-call link), email link, "Back to Home" button
    - Set PageMeta with title "Thank You", noindex meta tag (don't index thank-you pages)
    - Premium feel: clean layout, confidence-building messaging, clear next steps

    **4. Update `src/App.tsx`:**
    - Add lazy import: `const ThankYou = lazy(() => import('./pages/ThankYou'))`
    - Add route inside the Layout route group: `<Route path="thank-you" element={<ThankYou />} />`
    - Place it before the catch-all `*` route
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - `npm run dev` -- navigate to /contact, form renders with all fields
    - Fill in valid data + submit -- console shows form data (since formspreeId is empty, falls back to console.log) and redirects to /thank-you
    - Leave a required field empty and blur away -- inline error appears with proper aria attributes
    - Try invalid email format -- "Please enter a valid email address" error shows on blur
    - /thank-you page renders with next steps, business hours, phone link
  </verify>
  <done>
    Contact form uses React Hook Form + Zod with inline validation on touch. Honeypot spam protection present but invisible. Form submits through form-handler (currently logs to console since no Formspree ID configured). Successful submission redirects to /thank-you page with useful next steps. All required fields validate with accessible error announcements (aria-invalid, role="alert").
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Navigate to /contact -- form renders with all fields, styled consistently with current design
3. Submit empty form -- inline errors appear on required fields
4. Fill valid data + submit -- redirects to /thank-you
5. /thank-you page shows next steps, phone CTA, business hours
6. Honeypot field is not visible or focusable
7. Screen reader audit: error messages announced via role="alert", fields marked aria-invalid
8. `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
- Contact form validates all fields inline using RHF + Zod on first blur
- Form submits to configured backend (Formspree/webhook/none) via form-handler.ts
- Honeypot field present but invisible to users
- Successful submission redirects to /thank-you page
- Thank-you page shows next steps, emergency contact, business hours
- Error state on submission failure shows retry-friendly message
- Loading state (disabled button + "Sending...") during submission
- All new files pass TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-generation/03-01-SUMMARY.md`
</output>
