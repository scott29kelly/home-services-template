---
phase: 07-foundation-verification-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/REQUIREMENTS.md
  - src/lib/blog.ts
  - .planning/STATE.md
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06

must_haves:
  truths:
    - "CFG-01 through CFG-06 acceptance criteria checkboxes in REQUIREMENTS.md are checked for all verified criteria"
    - "SEO-01 checkboxes confirmed as already checked (no update needed if already [x])"
    - "getPostsByTag() dead code removed from src/lib/blog.ts"
    - "STATE.md updated to reflect Phase 07 completion status"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated acceptance criteria checkboxes for CFG-01 through CFG-06"
      contains: "[x]"
    - path: "src/lib/blog.ts"
      provides: "Blog utilities without dead code"
  key_links:
    - from: ".planning/phases/01-foundation-config-refactor/01-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Verification findings drive which checkboxes get checked"
      pattern: "CFG-0[1-6]"
---

<objective>
Update REQUIREMENTS.md traceability checkboxes based on Plan 01 verification findings, remove confirmed dead code, and update project state.

Purpose: Verification findings from Plan 01 must be reflected in the project's traceability documents. Dead code identified in the milestone audit must be removed. Project state must be updated to close Phase 07.

Output: Updated REQUIREMENTS.md with CFG checkboxes checked, cleaned `blog.ts`, updated STATE.md.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-foundation-verification-cleanup/07-01-SUMMARY.md
@.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md checkboxes for CFG-01 through CFG-06</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read the completed `01-VERIFICATION.md` from Plan 01 to get the per-criterion pass/fail results.

For each CFG requirement (CFG-01 through CFG-06) in `REQUIREMENTS.md`:

1. Change each acceptance criterion `- [ ]` to `- [x]` ONLY for criteria that were verified as PASS in 01-VERIFICATION.md
2. Leave `- [ ]` for criteria that were FAIL (these represent documented gaps that are deferred)
3. Add `[COMPLETE]` to the requirement heading if ALL criteria pass, or leave as-is if partial

Based on research pre-verification findings, expected updates:

**CFG-01** (5 criteria): All 5 PASS — check all 5 boxes, add `[COMPLETE]` to heading
**CFG-02** (5 criteria): 4-5 PASS (assistant nav gating is the gap) — check passing boxes. If 4/5, do NOT add `[COMPLETE]`
**CFG-03** (5 criteria): All 5 PASS — check all 5 boxes, add `[COMPLETE]`
**CFG-04** (5 criteria): All 5 PASS — check all 5 boxes, add `[COMPLETE]`
**CFG-05** (4 criteria): 3 PASS, 1 FAIL (phone CTA) — check 3, leave phone CTA unchecked
**CFG-06** (3 criteria): 2 PASS, 1 PARTIAL (beforeAfter flag missing) — check 2, leave flag criterion unchecked

IMPORTANT: Use the ACTUAL findings from 01-VERIFICATION.md, not the predicted outcomes above. The predictions are guidance only.

Also verify SEO-01 section: confirm all 7 checkboxes are already `[x]`. If any are `[ ]`, update them. If all are already checked, no changes needed — document this in the summary.

Do NOT modify any other sections of REQUIREMENTS.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); const cfgSection=c.substring(c.indexOf('## Category: Infrastructure'),c.indexOf('## Category: Visual')); const checked=(cfgSection.match(/- \[x\]/g)||[]).length; const unchecked=(cfgSection.match(/- \[ \]/g)||[]).length; console.log('CFG checkboxes: '+checked+' checked, '+unchecked+' unchecked'); if(checked<20){console.error('Expected at least 20 checked CFG criteria');process.exit(1)} console.log('OK')"</automated>
    <manual>Open REQUIREMENTS.md and verify CFG section checkboxes match 01-VERIFICATION.md findings</manual>
  </verify>
  <done>REQUIREMENTS.md CFG-01 through CFG-06 checkboxes updated to reflect verification findings. SEO-01 confirmed already checked. Only PASS criteria are checked; FAIL criteria remain unchecked as documented gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and update project state</name>
  <files>src/lib/blog.ts, .planning/STATE.md</files>
  <action>
**Dead code removal:**

Remove the `getPostsByTag()` function from `src/lib/blog.ts`. This includes:
- The JSDoc comment block above the function (lines ~89-91)
- The function definition (lines ~92-94)

The function is confirmed dead: exported but imported nowhere in `src/`. Its use case is covered by inline tag filtering in `ResourcesIndex.tsx` via `useSearchParams`.

After removal, verify the file still exports all other functions correctly. Run `npx tsc --noEmit` to confirm no TypeScript errors.

**Already-resolved tech debt documentation:**

The noindex meta tag placement in ThankYou.tsx was already fixed during Phase 05 (STATE.md decision log 2026-02-24). Verify `src/pages/ThankYou.tsx` has `noindex` as a prop on `PageMeta` (not a standalone meta tag). No code changes needed — this is verification only.

**Update STATE.md:**

Update `.planning/STATE.md` to reflect Phase 07 status:
1. Update "Current Phase" section to show Phase 7 in progress / complete
2. Add Phase 7 to Phase Status table with status COMPLETE and today's date (2026-02-25)
3. Add Plans Completed: 07-01, 07-02
4. Update Milestone Progress: requirements completed count (add CFG-01 through CFG-06 to the completed list if not already there — check first, they may already be listed)
5. Update Session Continuity with current session info
6. Update Next Action to point to Phase 6 (Integration & Polish) or final milestone review

Note: CFG-01 through CFG-06 may already appear in the "Requirements completed" list in STATE.md since they were marked complete in Phase 1. If so, do NOT duplicate them. The key state change is recording Phase 7 as COMPLETE.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const blog=fs.readFileSync('src/lib/blog.ts','utf8'); if(blog.includes('getPostsByTag')){console.error('Dead code still present');process.exit(1)} console.log('Dead code removed'); const state=fs.readFileSync('.planning/STATE.md','utf8'); if(!state.includes('07') || !state.includes('COMPLETE')){console.error('STATE.md not updated');process.exit(1)} console.log('STATE.md updated')"</automated>
    <manual>Verify blog.ts no longer exports getPostsByTag, and STATE.md shows Phase 7 complete</manual>
  </verify>
  <done>getPostsByTag() dead code removed from blog.ts. TypeScript compiles cleanly. ThankYou.tsx noindex confirmed already correct. STATE.md updated with Phase 7 completion status.</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md CFG-01 through CFG-06 checkboxes reflect verification findings (at least 20/27 checked)
- SEO-01 checkboxes confirmed as already `[x]` (no spurious changes)
- `src/lib/blog.ts` no longer contains `getPostsByTag` function
- `npx tsc --noEmit` passes after dead code removal
- STATE.md shows Phase 7 as COMPLETE with today's date
- No scope expansion beyond listed tech debt items
</verification>

<success_criteria>
- REQUIREMENTS.md traceability updated for all 6 CFG requirements based on actual verification evidence
- Dead code (`getPostsByTag`) removed, TypeScript still compiles
- ThankYou.tsx noindex confirmed already resolved (no changes made)
- STATE.md reflects Phase 07 completion
- Project is ready for Phase 6 (Integration & Polish)
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-verification-cleanup/07-02-SUMMARY.md`
</output>
