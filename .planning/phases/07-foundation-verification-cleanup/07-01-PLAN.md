---
phase: 07-foundation-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation-config-refactor/01-VERIFICATION.md
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06

must_haves:
  truths:
    - "CFG-01 through CFG-06 each have per-checkbox pass/fail status documented with file path evidence"
    - "01-VERIFICATION.md exists in the Phase 01 directory with summary table showing overall pass rate"
    - "Known gaps (CFG-05 phone CTA, CFG-06 beforeAfter flag, CFG-02 assistant nav gating) are documented with severity ratings"
    - "Discovered issues section captures any additional findings without expanding scope"
  artifacts:
    - path: ".planning/phases/01-foundation-config-refactor/01-VERIFICATION.md"
      provides: "Formal verification report for Phase 01 CFG requirements"
      contains: "CFG-01"
  key_links:
    - from: ".planning/phases/01-foundation-config-refactor/01-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirement IDs and acceptance criteria references"
      pattern: "CFG-0[1-6]"
---

<objective>
Formally verify Phase 01 pre-GSD work (CFG-01 through CFG-06) against acceptance criteria and produce a VERIFICATION.md report.

Purpose: Phase 01 was completed before the GSD workflow was adopted. These 6 requirements have no formal verification artifact. This plan inspects the codebase against each acceptance criterion, documents evidence of satisfaction or gaps, and creates the missing verification report.

Output: `.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md` with per-requirement, per-checkbox verification evidence.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-foundation-verification-cleanup/07-RESEARCH.md
@.planning/phases/02-visual-polish-performance/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CFG-01 through CFG-03 against acceptance criteria</name>
  <files>.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md</files>
  <action>
Create the directory `.planning/phases/01-foundation-config-refactor/` if it does not exist.

Inspect the codebase to verify each acceptance criterion for CFG-01, CFG-02, and CFG-03. For each criterion, determine PASS or FAIL and record the specific file paths and brief code evidence.

**CFG-01: Modular Config System (5 criteria)**
Verify:
1. Config split into 10+ domain-specific files — list files in `src/config/`
2. Zod schemas validate all config at import time — check `src/config/schema.ts` and `src/config/index.ts` for validation calls
3. Invalid config produces clear error messages — check the `validate()` function error handling pattern
4. Existing `SITE` import still works — check `src/config/site.ts` barrel export
5. All existing components render correctly — confirm TypeScript compiles (`npx tsc --noEmit`)

**CFG-02: Config-Driven Navigation (5 criteria)**
Verify:
1. Header nav links generated from config — check `Header.tsx` for `getNavLinks()` usage, no hardcoded arrays
2. Footer links generated from config — check `Footer.tsx` for `getFooterServiceLinks()`/`getFooterCompanyLinks()`, no hardcoded arrays
3. Services dropdown populated from services config — check `navigation.ts` services mapping
4. Adding a service auto-appears — trace `services.ts` through `navigation.ts` generation functions
5. Feature-flagged pages appear/disappear — check blog/financingCalculator flags gate nav entries. Note: research found `features.assistant` does NOT gate Ava nav link — document this as a PARTIAL/gap per CONTEXT.md failure handling (minor: document and defer)

**CFG-03: Config-Driven Routing (5 criteria)**
Verify:
1. Routes generated from `services.ts` config array — check `App.tsx` and `route-generator.ts`
2. Single `ServicePage.tsx` renders any service by slug — check `ServicePage.tsx` uses `useLocation()` or `useParams()` for slug lookup
3. Adding a new service requires only config entry — trace that no component code is service-specific
4. Existing service URLs still work — confirm `/roofing`, `/siding`, `/storm-damage` slugs in `services.ts`
5. 404 catch-all route exists — check `App.tsx` for `path="*"` route

Begin creating `01-VERIFICATION.md` with:
- Frontmatter (phase, verified timestamp, status placeholder, score placeholder)
- Phase goal statement
- Summary table (populated after all verification is done in Task 2)
- CFG-01, CFG-02, CFG-03 sections with criterion tables showing pass/fail and evidence

Follow the format established in `.planning/phases/02-visual-polish-performance/02-VERIFICATION.md`. Use the table structure with columns: Criterion | Status | Evidence.

Run `npx tsc --noEmit` as part of CFG-01 criterion 5 verification. If TypeScript errors exist, document them but note they may be pre-existing.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const p = '.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md'; if(!fs.existsSync(p)){process.exit(1)} const c=fs.readFileSync(p,'utf8'); const checks=['CFG-01','CFG-02','CFG-03','PASS','Evidence']; const missing=checks.filter(k=>!c.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('OK: CFG-01 through CFG-03 documented')"</automated>
    <manual>Open 01-VERIFICATION.md and confirm each CFG-01/02/03 criterion has a PASS or FAIL with evidence file paths</manual>
  </verify>
  <done>CFG-01, CFG-02, and CFG-03 sections written in 01-VERIFICATION.md with per-checkbox pass/fail and file path evidence. Known gaps (CFG-02 assistant nav gating) documented with minor severity.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CFG-04 through CFG-06 and finalize VERIFICATION.md</name>
  <files>.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md</files>
  <action>
Continue verification for CFG-04, CFG-05, and CFG-06 in the same `01-VERIFICATION.md` file.

**CFG-04: Content Migration to Config (5 criteria)**
Verify:
1. Testimonials data in `testimonials.ts`, not in component files — grep for testimonial data arrays, confirm only in config
2. `featured` flag controls homepage vs. full-page display — check `testimonials.ts` for `featured` field and components for filtering
3. Project/portfolio data in `projects.ts`, not in `Projects.tsx` — check imports
4. FAQ data in respective service configs — check `services.ts` for `faqs` field per service
5. Team member data in `team.ts`, not in `About.tsx` — check `About.tsx` imports from `team.ts`

**CFG-05: 404 Page (4 criteria)**
Verify:
1. Unknown URLs render a styled 404 page inside Layout — check `App.tsx` catch-all route and `NotFound.tsx` rendering
2. 404 page includes navigation links to key pages — check for Link components in `NotFound.tsx`
3. 404 page includes phone CTA — research found this is MISSING. Verify by reading `NotFound.tsx` and confirm no `tel:` link or phone number. Document as FAIL with severity minor per CONTEXT.md failure handling
4. Page has appropriate meta tags (noindex) — check for `PageMeta` with `noindex` prop

**CFG-06: Feature Flags System (3 criteria)**
Verify:
1. `features.blog`, `features.assistant`, `features.cityPages`, `features.beforeAfter` flags exist — read `src/config/features.ts`. Research found `beforeAfter` is MISSING. Document as FAIL. Note `financingCalculator` and `onlineBooking` exist but are not in the criteria list; `beforeAfter` is in the criteria but does not exist.
2. Disabling a feature removes its route, nav item, and related UI — trace blog/cityPages/financingCalculator flags through `App.tsx` and `navigation.ts`. Note: `features.assistant` does not gate route or nav (per CFG-02 gap finding — cross-reference)
3. All features enabled by default for demo — check all flag values are `true`

After verifying CFG-04/05/06:

1. Complete the summary table at the top of 01-VERIFICATION.md with overall pass rates per requirement
2. Set the frontmatter `status` field: if all criteria pass use `passed`, if any fail use `partial`
3. Set the frontmatter `score` field: `N/M checkboxes verified` (count total PASS vs total criteria)
4. Add a "Discovered Issues" section at the bottom if any non-criterion issues were found during verification
5. Add an "Already Resolved Items" section documenting:
   - noindex meta tag in ThankYou.tsx — already fixed during Phase 05 (confirm by reading the file)
   - SEO-01 checkboxes in REQUIREMENTS.md — already all `[x]` (confirm by reading REQUIREMENTS.md SEO-01 section)

Expected outcome based on research: ~24/27 criteria PASS, with known gaps:
- CFG-02: assistant nav link not gated by feature flag (minor)
- CFG-05: 404 page missing phone CTA (minor)
- CFG-06: `beforeAfter` flag missing from features.ts (minor)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const p = '.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md'; const c=fs.readFileSync(p,'utf8'); const checks=['CFG-04','CFG-05','CFG-06','Summary','status:','score:']; const missing=checks.filter(k=>!c.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1)} const hasAllSix=['CFG-01','CFG-02','CFG-03','CFG-04','CFG-05','CFG-06'].every(r=>c.includes(r)); if(!hasAllSix){console.error('Not all 6 CFG requirements documented');process.exit(1)} console.log('OK: All 6 CFG requirements documented with summary')"</automated>
    <manual>Verify the summary table has accurate pass rates, gaps have severity ratings, and discovered issues section exists</manual>
  </verify>
  <done>01-VERIFICATION.md complete with all 6 CFG requirements verified per-checkbox, summary table with pass rates, gaps documented with severity, and already-resolved items noted. Frontmatter status and score reflect actual findings.</done>
</task>

</tasks>

<verification>
- 01-VERIFICATION.md exists at `.planning/phases/01-foundation-config-refactor/01-VERIFICATION.md`
- All 6 CFG requirements (CFG-01 through CFG-06) have individual sections with per-criterion evidence
- Summary table shows pass rate per requirement
- Known gaps documented with severity ratings (minor/critical)
- Frontmatter has valid status (passed/partial) and score
- File follows the format established by Phase 02 VERIFICATION.md
</verification>

<success_criteria>
- 01-VERIFICATION.md created with frontmatter and all 6 CFG requirement sections
- Each acceptance criterion has PASS or FAIL status with file path evidence
- Summary shows overall pass rate (~24/27 expected)
- Gaps have severity ratings per CONTEXT.md failure handling decisions
- No scope expansion (gaps documented, not fixed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-verification-cleanup/07-01-SUMMARY.md`
</output>
