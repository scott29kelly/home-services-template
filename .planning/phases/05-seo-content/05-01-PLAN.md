---
phase: 05-seo-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/PageMeta.tsx
  - src/config/seo.ts
  - src/config/testimonials.ts
  - src/config/schema.ts
  - src/config/index.ts
  - src/pages/TestimonialsPage.tsx
autonomous: true
requirements: [SEO-05, SEO-06]

must_haves:
  truths:
    - "Every page has og:image, Twitter card meta tags, and canonical URL in head"
    - "Social share previews show correct image and description on Facebook/Twitter"
    - "Testimonials have rating, citySlug, and serviceSlug fields for filtering"
    - "Google Reviews placeholder section visible on testimonials page with setup instructions"
  artifacts:
    - path: "src/components/ui/PageMeta.tsx"
      provides: "Complete meta tag system with OG, Twitter, canonical"
      contains: "og:image"
    - path: "src/config/seo.ts"
      provides: "SEO defaults including ogImage path and twitter card type"
      contains: "ogImage"
    - path: "src/config/testimonials.ts"
      provides: "Testimonials with rating, citySlug, serviceSlug, featured fields"
      contains: "citySlug"
  key_links:
    - from: "src/components/ui/PageMeta.tsx"
      to: "src/config/seo.ts"
      via: "import seo defaults"
      pattern: "import.*seo.*from.*config"
    - from: "src/components/ui/PageMeta.tsx"
      to: "src/config/company.ts"
      via: "import company.url for absolute URLs"
      pattern: "company\\.url"
---

<objective>
Enhance the meta tag system for complete social sharing support and update the reviews/testimonials config with filtering fields and a Google Reviews placeholder.

Purpose: Every page needs og:image, Twitter cards, and canonical URLs for social sharing and SEO. Testimonials need city/service filtering for upcoming city pages and service pages.
Output: Enhanced PageMeta.tsx, updated seo.ts config, updated testimonials with new fields, Google Reviews placeholder.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-content/05-RESEARCH.md
@src/components/ui/PageMeta.tsx
@src/config/seo.ts
@src/config/testimonials.ts
@src/config/schema.ts
@src/config/company.ts
@src/config/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance PageMeta with OG image, Twitter cards, and canonical URLs</name>
  <files>src/components/ui/PageMeta.tsx, src/config/seo.ts</files>
  <action>
**src/config/seo.ts** — Add fields for complete SEO defaults:
- Add `twitterCardType: 'summary_large_image'` field
- Keep existing `titleTemplate`, `defaultTitle`, `defaultDescription`, `ogImage` fields
- `ogImage` value stays as `/images/og-default.webp` (already exists)

**src/components/ui/PageMeta.tsx** — Extend to full social sharing support:
1. Update interface to `PageMetaProps`:
   - Keep: `title: string`, `description: string`, `path?: string`
   - Add: `ogImage?: string` (per-page OG image override — relative path or absolute URL)
   - Add: `noindex?: boolean` (for thank-you, 404 pages)

2. Import `company` from `../../config/company` and `seo` from `../../config/seo`
   - Replace `SITE` import with direct `company` import (only need `company.name` and `company.url`)

3. Build absolute OG image URL:
   ```typescript
   const imageUrl = (() => {
     const src = ogImage || seo.ogImage
     return src.startsWith('http') ? src : `${company.url}${src}`
   })()
   ```

4. Add these meta/link elements to the returned JSX:
   - `<link rel="canonical" href={pageUrl} />` (React 19 hoists to head)
   - `<meta property="og:image" content={imageUrl} />`
   - `<meta property="og:image:width" content="1200" />`
   - `<meta property="og:image:height" content="630" />`
   - `<meta name="twitter:card" content={seo.twitterCardType} />`
   - `<meta name="twitter:title" content={fullTitle} />`
   - `<meta name="twitter:description" content={description} />`
   - `<meta name="twitter:image" content={imageUrl} />`
   - If `noindex`: `<meta name="robots" content="noindex, nofollow" />`

5. Keep existing `<title>`, `<meta name="description">`, `<meta property="og:title">`, `<meta property="og:description">`, `<meta property="og:url">`, `<meta property="og:type">` tags.

6. Update `fullTitle` to use `company.name` instead of `SITE.name`, and `pageUrl` to use `company.url` instead of `SITE.url`.

CRITICAL: OG image URL MUST be absolute (starts with http). Social crawlers cannot resolve relative paths. The `imageUrl` builder handles this.

CRITICAL: Use `property` attribute for OG tags, `name` attribute for Twitter tags. This is correct per the specs.

7. Update existing pages that should use `noindex`:
   - `src/pages/ThankYou.tsx` — add `noindex` prop to its PageMeta call
   - `src/pages/NotFound.tsx` — add `noindex` prop to its PageMeta call
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check PageMeta.tsx has og:image, twitter:card, and canonical link tags</manual>
  </verify>
  <done>PageMeta renders og:image with absolute URL, twitter:card as summary_large_image, canonical link, and noindex support. seo.ts has twitterCardType field. ThankYou and NotFound pages have noindex.</done>
</task>

<task type="auto">
  <name>Task 2: Update testimonials config with filtering fields and Google Reviews placeholder</name>
  <files>src/config/testimonials.ts, src/config/schema.ts, src/config/index.ts, src/pages/TestimonialsPage.tsx</files>
  <action>
**src/config/testimonials.ts** — Extend the Testimonial interface:
1. Add optional fields to the `Testimonial` interface:
   - `rating?: number` (1-5 star rating; defaults to 5 when used in AggregateRating)
   - `featured?: boolean` (explicitly mark for homepage; currently handled by separate `featured` array)
   - `citySlug?: string` (filter: show on /service-areas/{slug})
   - `serviceSlug?: string` (filter: show on /{service-slug})

2. Add `rating` values to existing testimonial data:
   - All featured: `rating: 5`
   - All in `all` array: most `rating: 5`, a couple `rating: 4` for realism

3. Add `citySlug` values matching the testimonial locations to existing city slugs:
   - Michael R. (Anytown) → `citySlug: 'anytown'`
   - Jennifer T. (Springfield) → `citySlug: 'springfield'`
   - David & Susan K. (Riverside) → `citySlug: 'riverside'`
   - Robert H. (Fairview) → `citySlug: 'fairview'`
   - Lisa M. (Georgetown) → `citySlug: 'georgetown'`
   - Paul S. (Lakewood) → `citySlug: 'lakewood'`
   - Tom C. (Cedar Park) → `citySlug: 'cedar-park'`
   - Angela M. (Madison) → `citySlug: 'madison'`

4. Add `serviceSlug` values based on the service field:
   - service 'Roofing' → `serviceSlug: 'roofing'`
   - service 'Storm Repair' / 'Insurance' → `serviceSlug: 'storm-damage'`
   - service 'Siding' → `serviceSlug: 'siding'`
   - service 'Multi-Property' → no serviceSlug (general)

5. Add `featured: true` to all items in the `featured` array. This makes the `featured` flag explicit on the data so city/service pages can also use it.

6. Add a helper function below the config:
   ```typescript
   /** Filter testimonials by city slug */
   export function getTestimonialsByCity(slug: string): Testimonial[] {
     return testimonials.all.filter(t => t.citySlug === slug)
   }

   /** Filter testimonials by service slug */
   export function getTestimonialsByService(slug: string): Testimonial[] {
     return testimonials.all.filter(t => t.serviceSlug === slug)
   }
   ```

**src/config/schema.ts** — Update the testimonial schema:
- Add `rating: z.number().min(1).max(5).optional()` to `testimonialSchema`
- Add `featured: z.boolean().optional()` to `testimonialSchema`
- Add `citySlug: z.string().optional()` to `testimonialSchema`
- Add `serviceSlug: z.string().optional()` to `testimonialSchema`

**src/config/index.ts** — Re-export the new helper functions:
- Add to re-exports: `export { getTestimonialsByCity, getTestimonialsByService } from './testimonials'`

**src/pages/TestimonialsPage.tsx** — Add Google Reviews placeholder section:
- After the existing testimonials grid and before the CTA section, add a new section:
  - SectionHeading: "Google Reviews"
  - A styled card/container with a visual placeholder showing 2-3 sample review cards (not real data — styled to look like Google review snippets)
  - A clear callout box with text: "Replace this section with your Google Reviews widget. See your template documentation for setup instructions."
  - Use existing design tokens: bg-surface, border-border, rounded-2xl
  - The placeholder reviews should use star icons (from Lucide `Star`) and look like typical Google review cards
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check testimonials.ts has rating/citySlug/serviceSlug fields, TestimonialsPage has Google Reviews placeholder</manual>
  </verify>
  <done>Testimonials have rating, citySlug, serviceSlug, and featured fields. Helper functions filter by city and service. Zod schema validates new fields. Google Reviews placeholder visible on testimonials page with clear setup instructions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- PageMeta.tsx renders og:image, twitter:card, canonical link, and optional noindex
- seo.ts has ogImage and twitterCardType fields
- testimonials.ts has rating, citySlug, serviceSlug optional fields on all entries
- TestimonialsPage.tsx has Google Reviews placeholder section
- ThankYou.tsx and NotFound.tsx use noindex prop
</verification>

<success_criteria>
- All pages have complete OG and Twitter meta tags with absolute image URLs
- Canonical URLs auto-generated from company.url + path
- Testimonials filterable by city and service slug
- Google Reviews placeholder section is visible and has clear setup instructions
- All config validates with updated Zod schemas
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-content/05-01-SUMMARY.md`
</output>
