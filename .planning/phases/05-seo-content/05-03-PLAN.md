---
phase: 05-seo-content
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/config/service-areas.ts
  - src/config/schema.ts
  - src/config/features.ts
  - src/config/navigation.ts
  - src/config/index.ts
  - src/lib/route-generator.ts
  - src/pages/CityPage.tsx
  - src/pages/ServiceAreas.tsx
  - src/App.tsx
autonomous: true
requirements: [SEO-02]

must_haves:
  truths:
    - "Each city in config has its own page at /service-areas/{slug}"
    - "City pages have unique content (description, meta title, meta description per city)"
    - "City pages show available services, local testimonials, nearby areas with internal links"
    - "Service area index page links to all city pages"
    - "20-25 demo cities included with unique descriptions mentioning local context"
  artifacts:
    - path: "src/pages/CityPage.tsx"
      provides: "City page template driven by slug matching"
      contains: "useParams"
    - path: "src/config/service-areas.ts"
      provides: "CityConfig array with 20-25 cities, each with unique description"
      contains: "cityPages"
    - path: "src/lib/route-generator.ts"
      provides: "getCityRoutes() function for dynamic route generation"
      contains: "getCityRoutes"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/CityPage.tsx"
      via: "Route path service-areas/:slug"
      pattern: "service-areas/:slug"
    - from: "src/pages/CityPage.tsx"
      to: "src/config/service-areas.ts"
      via: "import cityPages and find by slug"
      pattern: "cityPages.*find"
    - from: "src/pages/CityPage.tsx"
      to: "src/config/testimonials.ts"
      via: "getTestimonialsByCity for local reviews"
      pattern: "getTestimonialsByCity"
    - from: "src/pages/ServiceAreas.tsx"
      to: "/service-areas/{slug}"
      via: "Link components to city pages"
      pattern: "Link.*service-areas/"
---

<objective>
Build config-driven city/service-area pages with unique content per city, local testimonials, nearby area links, and an updated index page.

Purpose: City pages are critical for local SEO — each city page targets "{service} in {city}" search queries. Unique descriptions prevent thin content penalties. Internal linking between nearby cities builds topical authority.
Output: CityPage.tsx template, redesigned service-areas.ts with 20-25 demo cities, updated ServiceAreas.tsx index.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-content/05-RESEARCH.md
@.planning/phases/05-seo-content/05-01-SUMMARY.md
@src/config/service-areas.ts
@src/config/schema.ts
@src/config/features.ts
@src/config/navigation.ts
@src/lib/route-generator.ts
@src/pages/ServiceAreas.tsx
@src/App.tsx
@src/config/testimonials.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign service-areas.ts with CityConfig and 20-25 demo cities</name>
  <files>src/config/service-areas.ts, src/config/schema.ts, src/config/features.ts, src/config/index.ts</files>
  <action>
**src/config/service-areas.ts** — Major redesign. Export BOTH old and new shapes:

1. Define the `CityConfig` interface:
   ```typescript
   export interface CityConfig {
     slug: string              // 'anytown' -> /service-areas/anytown
     name: string              // 'Anytown'
     state: string             // 'TX'
     description: string       // Unique 2-3 sentence description with LOCAL context
     metaTitle?: string        // Override: defaults to '{name} Roofing & Siding | {company}'
     metaDescription?: string  // Override: falls back to description[:160]
     nearby: string[]          // slugs of nearby cities for internal linking
     faqs?: { question: string; answer: string }[]  // city-specific FAQs (2-3 per city)
     isHQ?: boolean            // Marks headquarters city
   }
   ```

2. Create `cityPages: CityConfig[]` array with 20-25 demo cities:

   **Texas cities (12-14):**
   - `anytown` (isHQ: true) — HQ city. Description mentions being the home base, locally owned.
   - `springfield` — Mention local neighborhoods, growing community.
   - `riverside` — Mention proximity to river, weather exposure.
   - `fairview` — Mention newer developments, growing families.
   - `madison` — Mention established neighborhoods, mature trees.
   - `georgetown` — Mention historic downtown, older homes.
   - `lakewood` — Mention lakeside properties, moisture issues.
   - `cedar-park` — Mention rapidly growing suburb, new construction.
   - `oak-ridge` — Mention wooded areas, tree damage risk.
   - `westfield` — Mention open plains, wind exposure.
   - `summit-heights` — Mention elevated terrain, hail exposure.
   - `pleasant-valley` — Mention family-friendly community, HOA standards.

   **Oklahoma cities (8-10):**
   - `tulsa` — Mention Tulsa metro area, tornado alley exposure.
   - `norman` — Mention OU campus area, severe weather belt.
   - `edmond` — Mention upscale neighborhoods, high standards.
   - `broken-arrow` — Mention fastest-growing suburb, new builds and aging homes.
   - `lawton` — Mention military community (Fort Sill), extreme weather.
   - `moore` — Mention tornado history, storm-resistant construction demand.
   - `stillwater` — Mention OSU campus area, rental property maintenance.
   - `midwest-city` — Mention Tinker AFB proximity, military families.

   **CRITICAL: Each city description MUST be unique and mention specific local context** — not just city name swaps. Each description should be 2-3 sentences referencing local landmarks, neighborhoods, weather patterns, community characteristics, or geography that make the city unique.

3. Each city should have a `nearby` array of 2-4 nearby city slugs for internal linking. Group by geographic proximity within each state.

4. Each city should have 2-3 `faqs` items. These should be city-specific questions like:
   - "What roofing services do you offer in {city}?"
   - "How do I file an insurance claim for storm damage in {city}?"
   - "Do you offer free inspections in the {city} area?"
   Answers should reference local context (e.g., "{city} homeowners frequently deal with hail damage during spring storm season").

5. **PRESERVE** the old `serviceAreas` export for backward compatibility:
   ```typescript
   // Keep for ServiceAreas.tsx index page backward compatibility
   export const serviceAreas = {
     summary: 'Serving the Greater Metro Area',
     states: [
       {
         name: 'Texas',
         badge: 'HQ' as string | undefined,
         areas: cityPages.filter(c => c.state === 'TX').map(c => c.name + (c.isHQ ? ' (HQ)' : '')),
       },
       {
         name: 'Oklahoma',
         badge: undefined as string | undefined,
         areas: cityPages.filter(c => c.state === 'OK').map(c => c.name),
       },
     ],
   }
   ```

6. Add helper function:
   ```typescript
   export function getCityBySlug(slug: string): CityConfig | undefined {
     return cityPages.find(c => c.slug === slug)
   }
   ```

**src/config/schema.ts** — Add CityConfig schema validation:
- Add `cityConfigSchema` with all CityConfig fields
- Add `cityPagesSchema = z.array(cityConfigSchema).min(1)`
- Keep existing `serviceAreasSchema` (backward compat shape still validated)

NOTE: This modifies the serviceAreas section of schema.ts. Plan 01 modifies testimonials section, Plan 02 modifies projects section. No overlap.

**src/config/features.ts** — Add city pages feature flag:
- Add `cityPages: true` to the features object
- Add JSDoc comment: `/** Enable city/service-area pages (Phase 5) */`

**src/config/index.ts** — Wire up new exports:
- Import `cityPages as _cityPages` from './service-areas'
- Import `cityPagesSchema` from './schema'
- Add validation: `export const cityPages = validate(cityPagesSchema, _cityPages, 'cityPages')`
- Re-export: `export { getCityBySlug } from './service-areas'`
- Re-export type: `export type { CityConfig } from './service-areas'`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check service-areas.ts has 20+ cities, each with unique description and nearby array</manual>
  </verify>
  <done>service-areas.ts exports cityPages array with 20-25 CityConfig objects, each having unique description, nearby links, and FAQs. Old serviceAreas shape preserved for backward compatibility. Zod schema validates all city configs. Feature flag added. Index barrel exports new items.</done>
</task>

<task type="auto">
  <name>Task 2: Build CityPage template, wire routes, and update ServiceAreas index</name>
  <files>src/pages/CityPage.tsx, src/lib/route-generator.ts, src/pages/ServiceAreas.tsx, src/config/navigation.ts, src/App.tsx</files>
  <action>
**src/pages/CityPage.tsx** — Create city page template (approximately 180-220 lines):

1. Imports: `useParams`, `Link` from react-router-dom; `PageMeta` from UI; `getCityBySlug`, `cityPages`, `company`, `services`, `features` from config; `getTestimonialsByCity` from config; `SectionHeading`, `CTA`, `Hero` from components; `m` from framer-motion; `useScrollReveal` hook; `MapPin`, `Phone`, `ArrowRight`, `Star` from lucide-react.

2. Use `useParams<{ slug: string }>()` to get city slug. Look up city with `getCityBySlug(slug)`. If not found, show NotFound or navigate away.

3. Get city testimonials: `const cityTestimonials = getTestimonialsByCity(city.slug)`

4. Page structure:
   a. **PageMeta** — title: `city.metaTitle || \`${city.name} Roofing & Siding | ${company.name}\``, description: `city.metaDescription || city.description.slice(0, 160)`, path: `/service-areas/${city.slug}`

   b. **Hero section** (compact) — headline: `${city.name}`, highlightText: `Home Services`, subhead: city.description. Use the service-areas-hero.webp image.

   c. **Services section** — SectionHeading "Our Services in {city.name}". Grid of service cards (2-3 cols) showing each service from the `services` config with icon, name, and brief description. Each card links to the service page.

   d. **Testimonials section** (only if `cityTestimonials.length > 0`) — SectionHeading "What {city.name} Homeowners Say". Show testimonial cards with quote, name, rating stars. If no city-specific testimonials, skip this section entirely.

   e. **FAQ section** (only if `city.faqs?.length`) — SectionHeading "Frequently Asked Questions in {city.name}". Render FAQ accordion/list matching the pattern used on service pages (simple toggle with plus/minus or chevron icons). Each FAQ item has question as heading and answer as paragraph.

   f. **Nearby Areas section** — "Also Serving Nearby Areas" heading. Render `city.nearby` as Link components to `/service-areas/{nearbySlug}`. Look up each nearby slug in `cityPages` to get the display name. Style as pill/chip links.

   g. **CTA section** — Standard CTA component.

5. Use `m` components with `useScrollReveal` for section animations, consistent with other pages.

**src/lib/route-generator.ts** — Add city route generation:
1. Import `cityPages` from `../config/service-areas`
2. Import `features` from `../config/features`
3. Add `CityRoute` interface: `{ path: string; slug: string; name: string }`
4. Add `getCityRoutes()` function:
   ```typescript
   export function getCityRoutes(): CityRoute[] {
     if (!features.cityPages) return []
     return cityPages.map(c => ({
       path: `service-areas/${c.slug}`,
       slug: c.slug,
       name: c.name,
     }))
   }
   ```

**src/App.tsx** — Add city page routes:
1. Add lazy import: `const CityPage = lazy(() => import('./pages/CityPage'))`
2. Import `getCityRoutes` from route-generator (add to existing import)
3. Add `const cityRoutes = getCityRoutes()` below `serviceRoutes`
4. Add route block inside Layout, after service-areas route:
   ```jsx
   {cityRoutes.map((route) => (
     <Route key={route.path} path={route.path} element={<CityPage />} />
   ))}
   ```

   Alternatively, use a single parameterized route: `<Route path="service-areas/:slug" element={<CityPage />} />` — this is simpler and handles any slug. Use this approach.

**src/pages/ServiceAreas.tsx** — Update index page with city links:
1. Import `cityPages` from `../config/service-areas` and `Link` from react-router-dom
2. In the states grid section, change the area pills from plain `<span>` to `<Link to={`/service-areas/${citySlug}`}>` elements:
   - For each `state.areas` string, find the matching city in `cityPages` to get the slug
   - If a match is found, render as a Link with hover styling
   - If no match found (shouldn't happen with backward-compat), render as plain span
3. Alternatively, add a new section below the states grid showing all city pages as a list grouped by state, with links

**src/config/navigation.ts** — No changes needed (service-areas link already in nav). The cityPages feature flag is used in route-generator, not navigation.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate to /service-areas/anytown and verify city page renders with services, nearby links, and FAQ. Navigate to /service-areas and verify city links work.</manual>
  </verify>
  <done>CityPage.tsx renders city pages at /service-areas/{slug} with services, testimonials, FAQ, and nearby area links. ServiceAreas index page links to all city pages. Route registered in App.tsx. Cities with testimonials show local reviews. Internal linking between nearby cities functional.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- `/service-areas/anytown` renders city page with unique description, services grid, FAQ, and nearby links
- `/service-areas/tulsa` renders a different city with its own unique content
- `/service-areas` index page has clickable links to city pages
- Nearby area links on city pages navigate to other city pages
- City pages with matching testimonials show local reviews section
- Feature flag `cityPages: false` disables city routes
</verification>

<success_criteria>
- 20-25 city pages accessible at /service-areas/{slug}
- Each city page has unique content (not just city name swaps)
- City pages show available services, local testimonials, FAQ, and nearby areas
- Service area index page links to all city pages
- Internal linking between nearby cities creates SEO authority network
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-content/05-03-SUMMARY.md`
</output>
