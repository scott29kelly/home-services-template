---
phase: 05-seo-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/projects.ts
  - src/config/schema.ts
  - src/pages/ProjectDetail.tsx
  - src/pages/Projects.tsx
  - src/App.tsx
autonomous: true
requirements: [SEO-07]

must_haves:
  truths:
    - "Clicking a project card navigates to /portfolio/{slug} with full detail view"
    - "Project detail page shows title, location, category, description, images, and before/after slider"
    - "Featured projects are identifiable via config flag"
    - "Category filter on projects page works correctly with updated data"
  artifacts:
    - path: "src/pages/ProjectDetail.tsx"
      provides: "Individual project detail page template"
      contains: "useParams"
    - path: "src/config/projects.ts"
      provides: "Projects with slug, featured, description fields"
      contains: "slug"
  key_links:
    - from: "src/pages/Projects.tsx"
      to: "/portfolio/{slug}"
      via: "Link component on project cards"
      pattern: "Link.*to=.*portfolio"
    - from: "src/App.tsx"
      to: "src/pages/ProjectDetail.tsx"
      via: "Route path portfolio/:slug"
      pattern: "portfolio/:slug"
    - from: "src/pages/ProjectDetail.tsx"
      to: "src/config/projects.ts"
      via: "import projects config and find by slug"
      pattern: "projects\\.items\\.find"
---

<objective>
Enhance the portfolio system with routable project detail pages, slug-based URLs, and enriched project data.

Purpose: Template buyers need project portfolio pages with unique URLs for SEO value and social sharing. Each project gets a detail view with full description and before/after comparison.
Output: ProjectDetail.tsx page, updated projects.ts with slugs and descriptions, clickable project cards.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-content/05-RESEARCH.md
@src/config/projects.ts
@src/config/schema.ts
@src/pages/Projects.tsx
@src/App.tsx
@src/components/ui/BeforeAfterSlider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update projects config with slug, featured, and description fields</name>
  <files>src/config/projects.ts, src/config/schema.ts</files>
  <action>
**src/config/projects.ts** — Extend Project interface and data:

1. Update `Project` interface:
   ```typescript
   export interface Project {
     id: number
     slug: string              // NEW: 'complete-roof-replacement' -> /portfolio/complete-roof-replacement
     title: string
     location: string
     category: ProjectCategory
     detail: string
     image: string
     description?: string      // NEW: 2-3 sentence description for detail page
     featured?: boolean        // NEW: show on homepage portfolio section
     beforeImage?: string      // NEW: before image path for before/after comparison
     afterImage?: string       // NEW: after image path for before/after comparison
   }
   ```

2. Update all 10 project items with explicit `slug` fields (kebab-case from title):
   - id 1: `slug: 'complete-roof-replacement'`, `featured: true`, add `description`
   - id 2: `slug: 'vinyl-siding-transformation'`, `featured: true`, add `description`
   - id 3: `slug: 'hail-damage-repair'`, `featured: true`, add `description`
   - id 4: `slug: 'designer-shingle-upgrade'`, add `description`
   - id 5: `slug: 'siding-trim-package'`, add `description`
   - id 6: `slug: 'wind-damage-restoration'`, `featured: true`, add `description`
   - id 7: `slug: 'multi-family-roofing'`, add `description`
   - id 8: `slug: 'historic-home-restoration'`, add `description`
   - id 9: `slug: 'estate-roof-replacement'`, add `description`
   - id 10: `slug: 'emergency-tree-damage-repair'`, add `description`

3. Each `description` should be 2-3 sentences describing the project scope, challenges, and outcome. Make them unique and realistic for a home services company.

4. Link the first 2 `beforeAfter` items to their corresponding project items:
   - Project id 1 (roof replacement): `beforeImage: '/images/before-after-1-before.webp'`, `afterImage: '/images/before-after-1-after.webp'`
   - Project id 2 (siding): `beforeImage: '/images/before-after-2-before.webp'`, `afterImage: '/images/before-after-2-after.webp'`

5. Add a helper function:
   ```typescript
   export function getProjectBySlug(slug: string): Project | undefined {
     return projects.items.find(p => p.slug === slug)
   }

   export function getFeaturedProjects(): Project[] {
     return projects.items.filter(p => p.featured)
   }
   ```

**src/config/schema.ts** — Update the project schema:
- Add `slug: z.string().min(1).regex(/^[a-z0-9-]+$/, 'Slug must be lowercase with hyphens')` to `projectSchema`
- Add `description: z.string().optional()` to `projectSchema`
- Add `featured: z.boolean().optional()` to `projectSchema`
- Add `beforeImage: z.string().optional()` to `projectSchema`
- Add `afterImage: z.string().optional()` to `projectSchema`

NOTE: This task modifies the `projectSchema` section of schema.ts ONLY. Plan 01 modifies the `testimonialSchema` section. These are different sections of the file — no conflict.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check projects.ts has slug field on all items, description on all items</manual>
  </verify>
  <done>All 10 projects have slug, description fields. 4 projects marked featured. 2 projects linked to before/after images. Zod schema validates new fields. Helper functions exported.</done>
</task>

<task type="auto">
  <name>Task 2: Build ProjectDetail page and wire routing</name>
  <files>src/pages/ProjectDetail.tsx, src/pages/Projects.tsx, src/App.tsx</files>
  <action>
**src/pages/ProjectDetail.tsx** — Create new page (approximately 120-160 lines):

1. Import `useParams`, `Link` from react-router-dom, `useNavigate` from react-router-dom
2. Import `PageMeta` from UI, `projects` and `getProjectBySlug` from config
3. Import `BeforeAfterSlider` from `../components/ui/BeforeAfterSlider`
4. Import `SectionHeading`, `CTA`, `Hero` components
5. Import `m` from framer-motion, `useScrollReveal` hook
6. Import `ArrowLeft`, `MapPin`, `Tag` from lucide-react

Implementation:
- Use `useParams<{ slug: string }>()` to get slug from URL
- Look up project with `getProjectBySlug(slug)` — if not found, navigate to `/projects` or show 404
- Render:
  a. **PageMeta** with project title, description (or detail as fallback), path `/portfolio/${slug}`, ogImage (project.image)
  b. **Hero section** (compact) with project image as background
  c. **Project details section:**
     - Title (h1), location with MapPin icon, category badge
     - Description paragraph (or detail if no description)
     - Project detail text (e.g., "Architectural Shingles")
  d. **Before/After section** (only if `project.beforeImage && project.afterImage`):
     - SectionHeading "Before & After"
     - `<BeforeAfterSlider>` with the project's before/after images
  e. **Gallery section** — show the main project image prominently
  f. **Back to Projects link** — `<Link to="/projects">` with ArrowLeft icon
  g. **Related Projects** — show 2-3 other projects in the same category (exclude current), displayed as small cards with links to their detail pages
  h. **CTA section** at the bottom

**src/pages/Projects.tsx** — Make project cards clickable:
1. Import `Link` from react-router-dom
2. Wrap each project card in `<Link to={`/portfolio/${project.slug}`}>` (or use an `<a>` inside the card)
3. Add hover effect indicating clickability (the existing `group-hover:scale-105` on the image is good; add cursor-pointer if not already present)
4. Keep the existing filter functionality and Before & After section intact

**src/App.tsx** — Add portfolio detail route:
1. Add lazy import: `const ProjectDetail = lazy(() => import('./pages/ProjectDetail'))`
2. Add route inside the Layout Route, before the catch-all:
   `<Route path="portfolio/:slug" element={<ProjectDetail />} />`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate to /portfolio/complete-roof-replacement and verify detail page renders with title, description, and back link</manual>
  </verify>
  <done>ProjectDetail.tsx renders individual project pages at /portfolio/{slug}. Project cards in Projects.tsx are clickable links. Route registered in App.tsx. Before/after slider shows when project has before/after images. Related projects display at bottom.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- `/portfolio/complete-roof-replacement` renders project detail page
- `/portfolio/nonexistent` redirects to /projects or shows 404
- Project cards on /projects page are clickable and navigate to detail pages
- Before/after slider displays on projects that have beforeImage/afterImage
- Category filter still works on /projects page
</verification>

<success_criteria>
- All 10 projects have routable detail pages at /portfolio/{slug}
- Project detail pages show title, location, description, images, and before/after when available
- Projects page cards are clickable links to detail pages
- Featured projects identifiable via config flag
- Related projects section shows same-category projects
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-content/05-02-SUMMARY.md`
</output>
