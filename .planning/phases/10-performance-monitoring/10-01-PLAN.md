---
phase: 10-performance-monitoring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/vitals.ts
  - src/main.tsx
  - src/App.tsx
  - .env.example
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "Core Web Vitals (CLS, INP, LCP) are measured and logged to console in development"
    - "Vercel Speed Insights can be enabled via VITE_VERCEL_SPEED_INSIGHTS=true environment variable"
    - "Lighthouse Performance score is 90+ on production build"
    - "Lighthouse Accessibility score is 90+ on production build"
    - "Lighthouse SEO score is 90+ on production build"
  artifacts:
    - path: "src/lib/vitals.ts"
      provides: "Core Web Vitals reporting module"
      exports: ["reportWebVitals"]
    - path: ".env.example"
      provides: "Environment variable documentation for template users"
      contains: "VITE_VERCEL_SPEED_INSIGHTS"
  key_links:
    - from: "src/main.tsx"
      to: "src/lib/vitals.ts"
      via: "import { reportWebVitals } and call after render"
      pattern: "reportWebVitals\\(\\)"
    - from: "src/App.tsx"
      to: "@vercel/speed-insights/react"
      via: "conditional SpeedInsights component behind env guard"
      pattern: "VITE_VERCEL_SPEED_INSIGHTS.*SpeedInsights"
---

<objective>
Wire up Core Web Vitals monitoring via the `web-vitals` library and Vercel Speed Insights, then verify Lighthouse scores meet 90+ targets for Performance, Accessibility, and SEO.

Purpose: Close the final v1.0 requirement gap (PERF-02) by adding real-user performance measurement and confirming the production build meets quality thresholds.
Output: `src/lib/vitals.ts` reporting module, wired `main.tsx` + `App.tsx`, `.env.example`, and verified Lighthouse scores.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-performance-monitoring/10-RESEARCH.md
@src/main.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install web-vitals and Speed Insights, create vitals reporter, wire into app</name>
  <files>
    src/lib/vitals.ts
    src/main.tsx
    src/App.tsx
    .env.example
    package.json
  </files>
  <action>
1. Install dependencies:
   ```bash
   npm install web-vitals @vercel/speed-insights
   ```

2. Create `src/lib/vitals.ts`:
   - Import `onCLS`, `onINP`, `onLCP`, and `type Metric` from `web-vitals`
   - Create a `logMetric(metric: Metric): void` function that:
     - In dev (`import.meta.env.DEV`): uses `console.groupCollapsed` with color-coded output (green for 'good', orange for 'needs-improvement', red for 'poor'), logs the full metric object, then `console.groupEnd()`
     - In production: no-op (future analytics hook point — add a comment indicating where to wire an analytics.track() call)
   - Export `reportWebVitals(): void` that calls `onCLS(logMetric)`, `onINP(logMetric)`, `onLCP(logMetric)`
   - Add JSDoc on `reportWebVitals` stating: "Call once from main.tsx after render — never inside components."
   - Follow the exact pattern from 10-RESEARCH.md Code Examples section

3. Modify `src/main.tsx`:
   - Add import: `import { reportWebVitals } from './lib/vitals'`
   - Add `reportWebVitals()` call AFTER the `createRoot(...).render(...)` block (not inside JSX)
   - Do NOT change existing imports or the render tree

4. Modify `src/App.tsx`:
   - Add import: `import { SpeedInsights } from '@vercel/speed-insights/react'`
   - Inside the `App()` function return, add the Speed Insights component AFTER the closing `</Suspense>` tag but still inside the returned fragment:
     ```tsx
     <>
       <Suspense fallback={<PageSkeleton />}>
         <Routes>...</Routes>
       </Suspense>
       {import.meta.env.VITE_VERCEL_SPEED_INSIGHTS === 'true' && <SpeedInsights />}
     </>
     ```
   - Use strict `=== 'true'` comparison (not truthy check) per research pitfall #5

5. Create `.env.example` at project root with:
   ```
   # ── Vercel Speed Insights ──────────────────────────────────
   # Set to 'true' to enable real-user monitoring on Vercel deployments.
   # Has no effect on non-Vercel hosts.
   VITE_VERCEL_SPEED_INSIGHTS=false

   # ── API Configuration ──────────────────────────────────────
   # Chat API endpoint (defaults to /api/chat in development)
   # VITE_API_URL=https://your-api.example.com

   # ── API Backend (server-side only, not exposed to browser) ─
   # GROQ_API_KEY=your-groq-api-key
   # ANTHROPIC_API_KEY=your-anthropic-api-key
   # ALLOWED_ORIGIN=https://your-domain.com
   ```
  </action>
  <verify>
    <automated>cd C:/Users/scott/Documents/home-services-template && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Run `npm run dev`, open browser console, navigate between pages — after ~5 seconds of interaction, CLS/INP/LCP entries should appear in console with color-coded labels</manual>
  </verify>
  <done>
    - `src/lib/vitals.ts` exports `reportWebVitals()` calling onCLS, onINP, onLCP
    - `src/main.tsx` calls `reportWebVitals()` after render
    - `src/App.tsx` conditionally renders `<SpeedInsights />` behind `VITE_VERCEL_SPEED_INSIGHTS` env guard
    - `.env.example` documents all environment variables
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Lighthouse audit on production build and verify 90+ scores</name>
  <files>
    .planning/phases/10-performance-monitoring/10-LIGHTHOUSE.md
  </files>
  <action>
1. Build and preview production bundle:
   ```bash
   npm run build
   npm run preview &
   ```
   Wait for the Vite preview server to start on port 4173.

2. Run Lighthouse CLI against the production build:
   ```bash
   npx lighthouse http://localhost:4173 --output=json --output-path=./lighthouse-report.json --chrome-flags="--headless --no-sandbox" --only-categories=performance,accessibility,seo
   ```
   IMPORTANT: Run against `localhost:4173` (preview), NOT `localhost:5173` (dev). Dev server produces artificially low scores per research pitfall #1.

3. Parse the Lighthouse JSON report to extract scores for Performance, Accessibility, and SEO. Each score is in `categories.{name}.score` (0-1 scale, multiply by 100).

4. Create `.planning/phases/10-performance-monitoring/10-LIGHTHOUSE.md` documenting:
   - Date of audit
   - URL tested
   - Scores achieved (Performance, Accessibility, SEO)
   - Whether each meets the 90+ target
   - If any score < 90: list the specific failing audits from the JSON report and note what would need to change (but do NOT expand Phase 10 scope to fix PERF-01 gaps like missing WebP hero images — document and defer)

5. If Lighthouse Performance is below 90 AND the cause is something within Phase 10 scope (not PERF-01 gaps): investigate and fix the specific issue. Common fixes:
   - Missing `aria-label` on interactive elements → add labels
   - Missing `lang` attribute → verify `<html lang="en">`
   - Image without dimensions → add width/height
   Do NOT address PERF-01 gaps (WebP hero images) — they are out of scope per research open question #2.

6. Clean up: stop the preview server, delete `lighthouse-report.json` (the scores are captured in 10-LIGHTHOUSE.md).

NOTE: If Chrome / Lighthouse CLI is not available in the environment, document this limitation and provide the manual steps for the user to run Lighthouse via Chrome DevTools instead. The vitals.ts implementation is the primary deliverable; Lighthouse verification is secondary.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-performance-monitoring/10-LIGHTHOUSE.md && echo "Lighthouse report exists" || echo "MISSING"</automated>
    <manual>Open Chrome DevTools > Lighthouse on http://localhost:4173. Verify Performance, Accessibility, and SEO scores are each 90+.</manual>
  </verify>
  <done>
    - Lighthouse report (10-LIGHTHOUSE.md) exists documenting scores for Performance, Accessibility, SEO
    - All three scores are 90+ OR specific failing audits are documented with remediation notes
    - No PERF-01 scope creep — any image format issues documented as existing gaps, not fixed
  </done>
</task>

</tasks>

<verification>
1. `src/lib/vitals.ts` exists and exports `reportWebVitals`
2. `src/main.tsx` imports and calls `reportWebVitals()` after render
3. `src/App.tsx` conditionally renders `<SpeedInsights />` behind env guard
4. `.env.example` documents `VITE_VERCEL_SPEED_INSIGHTS` and other env vars
5. `npm run build` succeeds without TypeScript errors
6. Lighthouse scores documented in `10-LIGHTHOUSE.md`
7. Web Vitals appear in browser console during development
</verification>

<success_criteria>
- PERF-02 acceptance criteria satisfied: web-vitals reports CLS/INP/LCP, Vercel Speed Insights integration available, Lighthouse 90+ verified
- Zero new runtime errors introduced
- Bundle size increase minimal (~2KB brotli for web-vitals)
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-monitoring/10-01-SUMMARY.md`
</output>
