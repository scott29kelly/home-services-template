---
phase: 04-blog-system
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/pages/ResourcesPost.tsx
  - src/content/blog/does-insurance-cover-storm-damage.md
  - src/content/blog/what-to-do-after-storm-damage.md
  - src/content/blog/document-roof-damage-insurance-claim.md
  - src/content/blog/signs-roof-needs-replacement.md
  - src/content/blog/how-long-does-roof-replacement-take.md
  - src/content/blog/best-roofing-materials-harsh-winters.md
  - src/content/blog/best-siding-winter-weather.md
  - src/content/blog/identify-siding-damage-early.md
  - src/content/blog/spring-home-inspection-checklist.md
  - src/content/blog/prepare-home-for-storm-season.md
  - src/content/blog/what-to-expect-roof-inspection.md
  - src/content/blog/energy-efficient-home-upgrades.md
  - images/blog/storm-damage-roof.jpg
  - images/blog/storm-damage-roof.webp
  - images/blog/after-storm-checklist.jpg
  - images/blog/after-storm-checklist.webp
  - images/blog/documenting-damage.jpg
  - images/blog/documenting-damage.webp
  - images/blog/roof-replacement-signs.jpg
  - images/blog/roof-replacement-signs.webp
  - images/blog/roof-replacement-timeline.jpg
  - images/blog/roof-replacement-timeline.webp
  - images/blog/roofing-materials-winter.jpg
  - images/blog/roofing-materials-winter.webp
  - images/blog/siding-winter.jpg
  - images/blog/siding-winter.webp
  - images/blog/siding-damage.jpg
  - images/blog/siding-damage.webp
  - images/blog/spring-inspection.jpg
  - images/blog/spring-inspection.webp
  - images/blog/storm-season-prep.jpg
  - images/blog/storm-season-prep.webp
  - images/blog/roof-inspection.jpg
  - images/blog/roof-inspection.webp
  - images/blog/energy-efficient-home.jpg
  - images/blog/energy-efficient-home.webp
autonomous: true
requirements: [SEO-01]
must_haves:
  truths:
    - "Individual post page renders markdown content at /resources/{slug} with proper typography"
    - "Post page shows hero image, title, date, and author byline"
    - "Post page includes BlogPosting JSON-LD structured data"
    - "Post page footer has a CTA section and 2-3 related post cards"
    - "12 demo posts are loaded and displayed on the listing page"
    - "Posts are sorted by date with newest first"
    - "Tag filtering on listing page works across all demo posts"
    - "Pagination on listing page works (12 posts / 6 per page = 2 pages)"
    - "Each post has proper meta tags (title, description, OG)"
  artifacts:
    - path: "src/pages/ResourcesPost.tsx"
      provides: "Individual post renderer with markdown, JSON-LD, CTA, related posts"
      min_lines: 80
    - path: "src/content/blog/"
      provides: "12 demo markdown posts with valid frontmatter"
    - path: "images/blog/"
      provides: "Cover images for blog posts (.jpg + .webp pairs)"
  key_links:
    - from: "src/pages/ResourcesPost.tsx"
      to: "src/lib/blog.ts"
      via: "import { getPostBySlug, getAllPosts }"
      pattern: "import.*getPostBySlug.*from.*lib/blog"
    - from: "src/pages/ResourcesPost.tsx"
      to: "react-markdown"
      via: "ReactMarkdown component rendering post.content"
      pattern: "ReactMarkdown"
    - from: "src/pages/ResourcesPost.tsx"
      to: "JSON-LD script tag"
      via: "dangerouslySetInnerHTML BlogPosting schema"
      pattern: "application/ld\\+json"
    - from: "src/content/blog/*.md"
      to: "src/lib/blog.ts"
      via: "import.meta.glob eager load"
      pattern: "import\\.meta\\.glob"
---

<objective>
Build the individual post page and create all demo content for the Resources section.

Purpose: Complete the Resources section by implementing the markdown post renderer with SEO structured data, post footer CTA, and related posts. Create 12 high-quality demo posts covering roofing, siding, storm damage, and seasonal maintenance topics — enough content to demonstrate pagination, tag filtering, and a premium reading experience.

Output: Fully functional Resources section with 12 demo posts, working listing page with 2 pages of content, and individual post pages with proper SEO markup.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blog-system/04-CONTEXT.md
@.planning/phases/04-blog-system/04-RESEARCH.md
@.planning/phases/04-blog-system/04-01-SUMMARY.md
@src/lib/blog.ts
@src/pages/ResourcesIndex.tsx
@src/components/ui/PageMeta.tsx
@src/config/company.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ResourcesPost page with markdown rendering, JSON-LD, and post footer</name>
  <files>
    src/pages/ResourcesPost.tsx
  </files>
  <action>
Create `src/pages/ResourcesPost.tsx` — the individual post reader page.

**Route integration:**
- Page is already routed at `/resources/:slug` from Plan 01 (App.tsx).
- Use `useParams<{ slug: string }>()` to get the slug from the URL.
- Look up the post with `getPostBySlug(slug)` from `src/lib/blog.ts`.
- If post not found, `<Navigate to="/resources" replace />`.

**Page meta:**
- Use `<PageMeta title={post.title} description={post.excerpt} path={"/resources/" + post.slug} />`.
- This gives correct `<title>`, description, and OG tags per post (CONTEXT.md locked decision).

**BlogPosting JSON-LD (per CONTEXT.md and RESEARCH.md Pattern 4):**
- Create a `BlogPostingSchema` component (inline in this file or extracted).
- Render a `<script type="application/ld+json" dangerouslySetInnerHTML>` with:
  ```json
  {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": post.title,
    "description": post.excerpt,
    "datePublished": post.date,
    "dateModified": post.date,
    "author": { "@type": "Organization", "name": post.author },
    "image": company.url + post.coverImage,
    "url": company.url + "/resources/" + post.slug,
    "keywords": post.tags.join(", ")
  }
  ```
- Import `company` from `../../config` for the base URL.
- Sanitize JSON with `.replace(/</g, '\\u003c')` to prevent script injection.

**Hero section:**
- Large hero/cover image at the top (full-width or max-w-4xl centered, rounded, aspect-video or similar).
- Image uses `loading="eager"` and `decoding="async"` (it is above the fold for this page).
- Below the image: post title as an `<h1>` with the site's heading font.
- Byline: date (formatted as "Month DD, YYYY" — manual string split, NOT `new Date()`) and author name. No estimated read time per CONTEXT.md locked decision.
- Tag pills displayed under the byline, linking back to `/resources?tag={tag}`.

**Markdown content (Medium-style clean layout per CONTEXT.md):**
- Centered content column: `max-w-3xl mx-auto px-4 sm:px-6`.
- Render markdown body with:
  ```tsx
  <div className="prose prose-lg prose-headings:font-heading prose-headings:text-navy prose-a:text-brand-blue prose-strong:text-navy max-w-none">
    <ReactMarkdown remarkPlugins={[remarkGfm]} components={{ img: ({ src, alt }) => <img src={src} alt={alt ?? ''} loading="lazy" decoding="async" className="rounded-xl w-full" /> }}>
      {post.content}
    </ReactMarkdown>
  </div>
  ```
- Import `ReactMarkdown` from `react-markdown` and `remarkGfm` from `remark-gfm`.
- The `prose` classes apply the typography plugin for proper heading hierarchy, link colors, blockquotes, code blocks, and vertical rhythm.
- Custom `img` component override adds lazy loading and rounded corners to inline images.

**Post footer (per CONTEXT.md locked decisions):**

1. **CTA section:** A visually distinct call-to-action block after the article content. Use the brand-blue background (or a contrasting section). Text like "Ready to protect your home?" with two buttons: "Schedule an Inspection" (links to `/contact`) and "Get a Free Quote" (links to `/contact`). Style consistent with existing CTA sections on other pages.

2. **Related posts:** Show 2-3 related post cards below the CTA.
   - Find related posts by matching tags: get all posts, filter to those sharing at least one tag with the current post, exclude the current post, take the first 3.
   - If fewer than 3 tag-matches, fill with the latest posts (excluding current).
   - Each related card: cover image thumbnail, title (linked to `/resources/{slug}`), and date.
   - Use a horizontal card layout (grid of 2-3 columns matching the listing page cards) with the heading "Related Resources" above.

**Styling:**
- Clean, focused, minimal distractions (Medium-style per CONTEXT.md).
- Consistent with the site's navy/brand-blue/surface color scheme.
- Ensure the page looks premium — this is the reading experience that represents the template quality.
  </action>
  <verify>
1. `npx tsc --noEmit` passes.
2. `npm run dev` — navigate to `/resources/{any-valid-slug}` (once demo posts exist) and verify:
   - Hero image renders.
   - Title, date, author byline visible.
   - Markdown body renders with proper typography (headings, bold, links styled).
   - JSON-LD script tag present in DOM (inspect element, search for `application/ld+json`).
   - CTA section visible below the article.
   - Related posts section shows 2-3 cards.
3. Navigate to `/resources/nonexistent-slug` — redirects to `/resources`.
4. No console errors.
  </verify>
  <done>
- ResourcesPost.tsx renders post content at /resources/{slug} with full typography
- Hero image, title, date, author byline displayed (no read time)
- BlogPosting JSON-LD schema injected per post
- CTA section with "Schedule an Inspection" / "Get a Free Quote" links
- 2-3 related post cards shown in footer
- Invalid slugs redirect to /resources listing
- PageMeta sets per-post title, description, and OG tags
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 12 demo blog posts and cover images</name>
  <files>
    src/content/blog/does-insurance-cover-storm-damage.md
    src/content/blog/what-to-do-after-storm-damage.md
    src/content/blog/document-roof-damage-insurance-claim.md
    src/content/blog/signs-roof-needs-replacement.md
    src/content/blog/how-long-does-roof-replacement-take.md
    src/content/blog/best-roofing-materials-harsh-winters.md
    src/content/blog/best-siding-winter-weather.md
    src/content/blog/identify-siding-damage-early.md
    src/content/blog/spring-home-inspection-checklist.md
    src/content/blog/prepare-home-for-storm-season.md
    src/content/blog/what-to-expect-roof-inspection.md
    src/content/blog/energy-efficient-home-upgrades.md
    images/blog/*.jpg
    images/blog/*.webp
  </files>
  <action>
**Step 1 — Create directory structure:**
```bash
mkdir -p src/content/blog
mkdir -p images/blog
```

**Step 2 — Create 12 demo markdown posts:**

Each post MUST follow the frontmatter schema defined in `src/lib/blog.ts`. Every post must have YAML frontmatter with: title, slug, date, excerpt, tags, coverImage, author ("Acme Home Services"), published (true).

**Content requirements (from CONTEXT.md locked decisions):**
- Length: 600-1000 words per post (medium length).
- Tone: Educational, helpful, practical — expert advice a homeowner would trust. NOT generic AI filler.
- SEO optimized: Target long-tail queries homeowners actually search.
- GEO optimized: Structure for AI model extraction/citation. Start each post with a direct answer in the first 40-60 words.
- Include a `## Frequently Asked Questions` section at the end of each post (2-3 Q&A pairs) for featured snippet targeting and GEO extraction.
- Structure with clear `##` headers that AI models can extract as standalone answers.
- Tags should cover: "roofing", "siding", "storm-damage", "insurance", "maintenance", "winter", "inspection", "energy-efficiency" (use 2-3 tags per post).

**Post roster (12 posts with staggered dates spanning ~6 months):**

1. `does-insurance-cover-storm-damage.md`
   - slug: "does-insurance-cover-storm-damage"
   - date: "2026-02-15"
   - tags: ["storm-damage", "insurance", "roofing"]
   - coverImage: "/images/blog/storm-damage-roof.webp"
   - Topic: Whether homeowners insurance covers storm damage, what's typically covered vs not, how to check your policy.

2. `what-to-do-after-storm-damage.md`
   - slug: "what-to-do-after-storm-damage"
   - date: "2026-02-01"
   - tags: ["storm-damage", "insurance", "maintenance"]
   - coverImage: "/images/blog/after-storm-checklist.webp"
   - Topic: Immediate steps after a storm — safety, documentation, temporary fixes, calling your insurer.

3. `document-roof-damage-insurance-claim.md`
   - slug: "document-roof-damage-insurance-claim"
   - date: "2026-01-18"
   - tags: ["insurance", "roofing", "storm-damage"]
   - coverImage: "/images/blog/documenting-damage.webp"
   - Topic: How to properly photograph and document roof damage for an insurance claim.

4. `signs-roof-needs-replacement.md`
   - slug: "signs-roof-needs-replacement"
   - date: "2026-01-05"
   - tags: ["roofing", "maintenance", "inspection"]
   - coverImage: "/images/blog/roof-replacement-signs.webp"
   - Topic: 7-8 warning signs a roof needs replacement vs repair. Age, curling shingles, granule loss, etc.

5. `how-long-does-roof-replacement-take.md`
   - slug: "how-long-does-roof-replacement-take"
   - date: "2025-12-20"
   - tags: ["roofing", "maintenance"]
   - coverImage: "/images/blog/roof-replacement-timeline.webp"
   - Topic: Timeline for a typical residential roof replacement, factors that affect duration.

6. `best-roofing-materials-harsh-winters.md`
   - slug: "best-roofing-materials-harsh-winters"
   - date: "2025-12-05"
   - tags: ["roofing", "winter", "maintenance"]
   - coverImage: "/images/blog/roofing-materials-winter.webp"
   - Topic: Comparing asphalt shingles, metal roofing, and slate for cold-climate performance.

7. `best-siding-winter-weather.md`
   - slug: "best-siding-winter-weather"
   - date: "2025-11-20"
   - tags: ["siding", "winter", "energy-efficiency"]
   - coverImage: "/images/blog/siding-winter.webp"
   - Topic: Best siding materials for winter weather and energy efficiency — vinyl, fiber cement, insulated options.

8. `identify-siding-damage-early.md`
   - slug: "identify-siding-damage-early"
   - date: "2025-11-05"
   - tags: ["siding", "maintenance", "inspection"]
   - coverImage: "/images/blog/siding-damage.webp"
   - Topic: How to spot siding damage early before it becomes costly — warping, gaps, moisture signs.

9. `spring-home-inspection-checklist.md`
   - slug: "spring-home-inspection-checklist"
   - date: "2025-10-20"
   - tags: ["maintenance", "inspection", "roofing", "siding"]
   - coverImage: "/images/blog/spring-inspection.webp"
   - Topic: Comprehensive spring checklist for roof, siding, gutters, foundation, and exterior.

10. `prepare-home-for-storm-season.md`
    - slug: "prepare-home-for-storm-season"
    - date: "2025-10-05"
    - tags: ["storm-damage", "maintenance", "roofing"]
    - coverImage: "/images/blog/storm-season-prep.webp"
    - Topic: Pre-storm preparation steps — tree trimming, gutter cleaning, loose materials, emergency kit.

11. `what-to-expect-roof-inspection.md`
    - slug: "what-to-expect-roof-inspection"
    - date: "2025-09-20"
    - tags: ["roofing", "inspection", "maintenance"]
    - coverImage: "/images/blog/roof-inspection.webp"
    - Topic: What happens during a professional roof inspection, what they check, how to prepare.

12. `energy-efficient-home-upgrades.md`
    - slug: "energy-efficient-home-upgrades"
    - date: "2025-09-05"
    - tags: ["energy-efficiency", "siding", "roofing"]
    - coverImage: "/images/blog/energy-efficient-home.webp"
    - Topic: Home exterior upgrades that improve energy efficiency — insulated siding, reflective roofing, sealing gaps.

**Step 3 — Source cover images:**

Download 12 free images from Unsplash or Pexels matching each post topic. Save each as both `.jpg` and `.webp` in `images/blog/`. Use descriptive kebab-case filenames matching the frontmatter `coverImage` path (but the frontmatter references the `.webp` version).

Image sourcing approach:
- Use `curl` or `wget` to download from Unsplash/Pexels API or direct URLs.
- If direct download is not feasible, create placeholder images using the existing project images as a fallback: copy relevant existing images (like `hero-roofing.webp`, `project-*.webp`) to `images/blog/` with appropriate names. The user can swap them later via frontmatter paths.
- Use `sharp` (already in devDependencies) to convert jpg to webp if needed:
  ```bash
  npx sharp -i images/blog/image.jpg -o images/blog/image.webp --format webp --quality 80
  ```
- Target image dimensions: ~1200x675 (16:9 aspect ratio) or similar landscape format suitable for hero/card display.

**Step 4 — Verify content loads:**
After creating all posts, run `npm run dev` and verify:
- `/resources` shows all 12 posts in the card grid.
- Pagination shows 2 pages (6 posts per page).
- Each post card has a cover image, title, date, and excerpt.
- Tag filtering works (clicking a tag filters posts).
- Clicking a post card navigates to `/resources/{slug}` and renders the full post.
  </action>
  <verify>
1. `ls src/content/blog/*.md | wc -l` returns 12.
2. `ls images/blog/*.webp | wc -l` returns at least 12.
3. `npm run dev` — `/resources` shows 12 post cards across 2 pages.
4. Each tag pill on the listing page filters posts correctly.
5. Clicking any post card navigates to the full post with rendered markdown.
6. Each post page has a visible JSON-LD script tag in the DOM.
7. Post footer shows CTA and related post cards.
8. `npm run build` succeeds (production build includes all markdown content).
9. No console warnings about invalid frontmatter.
  </verify>
  <done>
- 12 demo markdown posts created in src/content/blog/ with valid frontmatter
- Cover images present in images/blog/ (jpg + webp pairs)
- All posts appear on /resources listing page sorted by date (newest first)
- Pagination works (2 pages of 6 posts each)
- Tag filtering works across all 8 tag categories
- Each post renders at /resources/{slug} with full typography, JSON-LD, CTA, and related posts
- Production build succeeds with all content bundled
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (zero errors).
2. `/resources` listing page shows 12 posts in card grid with cover images.
3. Pagination works — page 1 shows 6 posts, page 2 shows 6 posts.
4. Tag filtering narrows posts correctly (e.g., `?tag=roofing` shows only roofing-tagged posts).
5. Each post page (`/resources/{slug}`) renders:
   - Hero image, title, date, author byline
   - Markdown body with proper typography (headings, bold, links, code)
   - BlogPosting JSON-LD schema in DOM
   - CTA section with contact links
   - 2-3 related post cards
6. Invalid slug redirects to listing page.
7. All 12 posts have valid frontmatter (no console warnings about invalid frontmatter).
8. Posts are sorted newest-first on listing page.
</verification>

<success_criteria>
- ResourcesPost.tsx renders individual posts with markdown typography, SEO structured data, CTA, and related posts
- 12 demo posts cover roofing, siding, storm damage, insurance, and seasonal maintenance topics
- Posts are 600-1000 words each with FAQ sections for GEO optimization
- Cover images exist for all posts
- Pagination and tag filtering work end-to-end with real content
- Complete Resources section looks premium and polished
</success_criteria>

<output>
After completion, create `.planning/phases/04-blog-system/04-02-SUMMARY.md`
</output>
