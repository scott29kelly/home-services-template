---
phase: 12-stickymobilecta-feature-flag-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/StickyMobileCTA.tsx
autonomous: true
requirements:
  - CFG-06
must_haves:
  truths:
    - "Setting features.assistant = false hides the Chat button in the StickyMobileCTA bar on mobile"
    - "Setting features.assistant = true (default) renders all three buttons (Call, Free Quote, Chat) in the StickyMobileCTA bar"
    - "Phone and Free Quote buttons remain visible regardless of features.assistant value"
  artifacts:
    - path: "src/components/ui/StickyMobileCTA.tsx"
      provides: "Feature-flag-gated Chat button"
      contains: "features.assistant"
  key_links:
    - from: "src/components/ui/StickyMobileCTA.tsx"
      to: "src/config/features.ts"
      via: "direct import of features object"
      pattern: "import.*features.*from.*config/features"
---

<objective>
Gate the Chat button in StickyMobileCTA behind `features.assistant` so that disabling the assistant feature consistently removes all chat-related UI across the entire application.

Purpose: Close INT-01 integration gap — Phase 11 gated AvaWidget in Layout.tsx but the Chat button in StickyMobileCTA was left unconditional, breaking E2E Flow 4 (feature flag off → consistent removal).
Output: Updated `StickyMobileCTA.tsx` with conditional Chat button rendering.
</objective>

<execution_context>
@C:/Users/scott/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scott/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stickymobilecta-feature-flag-fix/12-RESEARCH.md
@src/components/ui/StickyMobileCTA.tsx
@src/config/features.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate StickyMobileCTA Chat button behind features.assistant</name>
  <files>src/components/ui/StickyMobileCTA.tsx</files>
  <action>
In `src/components/ui/StickyMobileCTA.tsx`, make two changes:

1. **Add import** — After the existing `import { company } from '../../config/company'` line (line 5), add:
   ```typescript
   import { features } from '../../config/features'
   ```
   This follows the established direct-import pattern used in `Contact.tsx` and `Projects.tsx` (NOT the barrel import — consistent with the existing `company` import in this file).

2. **Wrap Chat button in conditional** — Find the `{/* Ava Chat */}` comment block (lines 75-82) and wrap the entire `<button>` element in a `{features.assistant && (...)}` expression:
   ```tsx
   {/* Ava Chat */}
   {features.assistant && (
     <button
       onClick={handleOpenAvaChat}
       className="flex flex-col items-center gap-1 px-3 py-1.5 rounded-xl text-navy hover:bg-slate-100 active:bg-slate-200 transition-colors min-w-[72px]"
     >
       <MessageSquare className="w-5 h-5 text-brand-blue" />
       <span className="text-[11px] font-semibold text-navy/70">Chat</span>
     </button>
   )}
   ```

**Do NOT:**
- Gate the entire StickyMobileCTA bar — only the Chat button is conditional
- Use CSS hiding (`hidden`, `display:none`) — the project pattern is DOM removal via conditional rendering
- Remove or modify the `handleOpenAvaChat` function — it is still called when the button renders
- Import from the barrel (`../../config`) — stay consistent with the existing `company` import from `../../config/company`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>
1. In src/config/features.ts, temporarily set `assistant: false`
2. Run `npm run dev`, open browser on mobile viewport (< 768px)
3. Scroll past 300px — sticky bar should appear with only Call and Free Quote (no Chat)
4. Restore `assistant: true` — sticky bar should show all three buttons (Call, Free Quote, Chat)
    </manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
- `features.assistant = false` removes the Chat button from StickyMobileCTA (DOM removal, not CSS hide)
- `features.assistant = true` (default) renders all three buttons unchanged from current behavior
- Phone and Free Quote buttons always render regardless of assistant flag
- TypeScript compiles cleanly with no errors
- INT-01 integration gap is closed; E2E Flow 4 passes
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no TypeScript errors introduced
2. `StickyMobileCTA.tsx` imports `features` from `../../config/features`
3. Chat button JSX is wrapped in `{features.assistant && (...)}`
4. Phone and Free Quote buttons are NOT wrapped in any conditional
5. No other files modified
</verification>

<success_criteria>
- TypeScript clean build
- Chat button conditionally rendered based on `features.assistant`
- E2E Flow 4 consistency: `features.assistant = false` removes ALL chat UI (AvaWidget + StickyMobileCTA Chat button)
- Zero functional regression when `features.assistant = true` (default state)
</success_criteria>

<output>
After completion, create `.planning/phases/12-stickymobilecta-feature-flag-fix/12-01-SUMMARY.md`
</output>
