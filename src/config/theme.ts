/**
 * Theme tokens referenced by config and components.
 * Actual CSS custom properties are defined in Tailwind / CSS — this file
 * provides a TypeScript reference for config-level color decisions.
 */
export const theme = {
  colors: {
    navy: '#0B1D3A',
    brandBlue: '#2563EB',
    safetyOrange: '#F97316',
  },
  fonts: {
    heading: 'Inter',
    body: 'Inter',
  },
}
